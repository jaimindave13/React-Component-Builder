# Cursor SDK credentials
# Get a user API key from https://cursor.com/dashboard/integrations
# Copy this file to .env.local and fill in the value, then restart `npm run dev`.
CURSOR_API_KEY=your_cursor_api_key_here

# Optional: override the model used for generation (defaults to composer-2.5)
CURSOR_MODEL_ID=composer-2.5